import { expect } from "chai";

describe("not a test", () => {
    it("function returns x", () => {
        const arrangeVar = 0;

        const result = 0;

        expect(result).to.equal(arrangeVar);
    });
});
